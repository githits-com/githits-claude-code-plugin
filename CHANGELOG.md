# Changelog

## [1.0.0] - 2026-02-24

### Added

- MCP server integration via the `githits` npm package (`npx -y githits mcp start`)
- Slash commands: search, login, logout, help, status
- Search skill for automatic code example discovery
- License filtering support (strict, yolo, custom)